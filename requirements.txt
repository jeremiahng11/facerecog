Django>=4.2,<5.2
face_recognition>=1.3.0
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
gunicorn>=21.2.0
whitenoise>=6.6.0
dj-database-url>=2.1.0
psycopg2-binary>=2.9.9
cloudinary>=1.36.0
django-cloudinary-storage>=0.3.0
qrcode>=7.4.0
openpyxl>=3.1.0

# Real-time WebSockets (activated when Redis is deployed)
channels>=4.0.0
channels-redis>=4.1.0
daphne>=4.0.0

# Async task queue (activated when Redis is deployed)
celery>=5.3.0
redis>=5.0.0

# Payment integration (activated when STRIPE_SECRET_KEY is set)
stripe>=9.0.0
